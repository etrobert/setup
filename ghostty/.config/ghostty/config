# https://ghostty.org/docs/config/reference

theme = Catppuccin Macchiato
macos-titlebar-style = hidden

command = /opt/homebrew/bin/bash

window-width = 80
window-height = 24

window-padding-x = 0
window-padding-y = 0
window-padding-balance = true

# Streaming 24 / Recording 14
font-size = 14

# configured by /terminal-setup inside claude
# Manually switched from shift+enter (recommended in Claude docs) because it interferes with nvim
keybind = alt+enter=text:\x1b\r

# Disable auto-updates - use brew instead
auto-update = off
